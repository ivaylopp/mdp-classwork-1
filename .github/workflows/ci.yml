name: CI

on: 
  push:
  workflow_dispatch:

jobs:
  call-workflow:
    uses: ivaylopp/mdp-classwork-ci/.github/workflows/ci.yml@java-1.0.0
    permissions:
      packages: write
    secrets: inherit
